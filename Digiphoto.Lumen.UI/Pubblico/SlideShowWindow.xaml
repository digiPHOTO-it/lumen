<Window x:Class="Digiphoto.Lumen.UI.Pubblico.SlideShowWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SlideShowWindow" Height="400" Width="500">
    

    <ItemsControl ItemsSource="{Binding Path=slidesVisibili}" >

<!--
        <UniformGrid Rows="{Binding Path=slideShow.righe}" Columns="{Binding Path=slideShow.colonne}" />
-->
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid Rows="2" Columns="3" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
            
            
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border BorderBrush="Red" BorderThickness="1">                        
                    <Image Source="{Binding Path=image}" />
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
    

</Window>
